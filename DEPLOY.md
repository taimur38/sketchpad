# Deployment

This site is hosted locally on this machine. The `dist/` directory is served directly.

To deploy changes:
1. Make your edits
2. Run `npm run build`
3. That's it — the site is live immediately

No GitHub Pages, no CI/CD, no merge to master needed.
